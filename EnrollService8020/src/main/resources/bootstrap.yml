server:
  port: 8020
spring:
  application:
    name: enroll-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: dev