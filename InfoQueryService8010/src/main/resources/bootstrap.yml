server:
  port: 8010
spring:
  application:
    name: infoquery-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        refresh-enabled: true
  profiles:
    active: dev